name: CI Workflow

on:
  push:
    branches:
      - ft/register-new-account
  pull_request:
    branches:
      - ft/register-new-account

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout the main repository
      - name: Checkout Main Repository
        uses: actions/checkout@v2

      # 2. Clone the backend repository
      - name: Clone Backend Repository
        run: |
          set -e
          git clone --branch ft/register-new-account https://github.com/d-rec/drec-origin.git projects/drec-origin
          echo "Backend repository cloned successfully."

      # 3. List the files in the repository to confirm structure
      - name: List Files in Backend Directory
        working-directory: projects/drec-origin
        run: |
          set -e
          ls -la
          echo "Backend directory contents listed."

      # 4. Set up Node.js
      - name: Set Up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '20'

      # 5. Install Rush and pnpm globally
      - name: Install Rush and pnpm
        run: |
          set -e
          npm install -g @microsoft/rush pnpm
          echo "Rush and pnpm installed successfully."

      # 6. Set up Docker Compose
      - name: Install Docker Compose
        run: |
          set -e
          sudo apt-get update
          sudo apt-get install -y docker-compose
          echo "Docker Compose installed successfully."

      # 7. Build and Start Services using Docker Compose
      - name: Build and Start Services
        working-directory: projects/drec-origin
        run: |
          set -e
          docker compose -f docker-compose.yml up --build -d
          echo "Docker services built and started in the backend folder."

      # 8. Wait for PostgreSQL and Backend to be Ready
      - name: Wait for Services to Be Ready
        working-directory: projects/drec-origin
        run: |
          set -e
          echo "Waiting for PostgreSQL..."
          timeout 180 bash -c "until nc -z localhost 5432; do echo 'Waiting for PostgreSQL...'; sleep 5; done"
          echo "PostgreSQL is ready."

          echo "Waiting for Backend..."
          timeout 180 bash -c "until curl -s http://localhost:3000/health | grep 'OK'; do echo 'Waiting for backend...'; sleep 5; done"
          echo "Backend is ready."

      # 9. Start the Backend
      - name: Start Backend (rush start:dev)
        working-directory: projects/drec-origin
        run: |
          set -e
          rush start:dev
          echo "Backend started in dev mode."

      # 10. Start the Frontend
      - name: Start Frontend (npm run start)
        working-directory: projects/drec-ui
        run: |
          set -e
          npm install
          npm run start &
          echo "Frontend started."

      # 11. Wait for the frontend to be ready
      - name: Wait for Frontend to be Ready
        run: |
          set -e
          timeout 180 bash -c "until curl -s http://localhost:3000 | grep 'OK'; do echo 'Waiting for frontend...'; sleep 5; done"
          echo "Frontend is ready."

      # 12. Install Cypress dependencies
      - name: Install Cypress Dependencies
        working-directory: cypress
        run: |
          set -e
          npm install
          echo "Cypress dependencies installed successfully."

      # 13. Run Cypress Tests
      - name: Run Cypress Tests
        run: |
          set -e
          npx cypress run --config-file=cypress.config.ts
          echo "Cypress tests completed successfully."

      # 14. Tear Down Docker Compose Environment
      - name: Tear Down Docker Compose Environment
        working-directory: projects/drec-origin
        run: |
          set -e
          docker compose down
          echo "Docker Compose environment torn down."
